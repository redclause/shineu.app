[package]
name = "shineu-app"
version = "0.1.0"
description = "ShineU — Influence OS"
authors = ["ShineU"]
edition = "2021"
rust-version = "1.77.2"

[lib]
name = "app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
